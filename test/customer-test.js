import chai from 'chai';
const expect = chai.expect;

import Customer from '../src/classes/Customer';
import customers from '../src/test-data/customer-data';
import bookings from '../src/data/sampleBookingsData';
import sampleRoomsData from '../src/data/sampleRoomsData';

describe('See if the tests are running', function() {
  it('should return true', function() {
    expect(true).to.equal(true);
  });

  it('should be able to select a date to book a room for myself', () => {



    expect(true).to.equal(true);
  });

  it('should return a list of room details for rooms available on only the booking date', () => {



    expect(true).to.equal(true);
  });
});
